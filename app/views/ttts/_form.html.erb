<%= form_for(@ttt) do |f| %>
  <% if @ttt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ttt.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @ttt.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :name %><br />
  <%= f.text_field :name %>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>